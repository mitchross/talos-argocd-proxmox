# Cluster-scoped pieces of the operator-free VolSync migration. Deployed
# once, not per-app. Eventual real home (NOT yet — branch only):
#   infrastructure/storage/volsync-backup-cluster/
# Today lives under docs/research/ so no ApplicationSet discovers it.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - mutating-admission-policy.yaml
# talos-patch.yaml is NOT a Kubernetes resource — it is an Omni machine-
# config patch and is applied via Omni, not kubectl. Do not list it here.
