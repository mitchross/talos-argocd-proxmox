# Deprecated: per-host Firewalla DNS overrides
#
# Private vanillax.me records are now managed by ExternalDNS in Technitium.
# Firewalla should keep serving DHCP/client DNS and forward the complete zone:
#
#   vanillax.me -> 192.168.10.15
#
# Enable subdomains and apply the rule to the required LAN/VLAN networks.
# Do not recreate the old per-host entries.
#
# Full runbook:
# docs/domains/networking/technitium-vanillax-me-migration.md
