# Renders the OCI chart via Kustomize helmCharts in infrastructure/controllers/kopiur-operator (8 CRDs + operator + webhook).
# Wave 2: the operator's volume populator + webhook must be healthy before any PVC carrying a kopiur dataSourceRef is created.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kopiur-operator
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
    argocd.argoproj.io/manifest-generate-paths: infrastructure/controllers/kopiur-operator
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: infrastructure
  source:
    repoURL: https://github.com/mitchross/talos-argocd-proxmox.git
    targetRevision: main
    path: infrastructure/controllers/kopiur-operator
  destination:
    server: https://kubernetes.default.svc
    namespace: kopiur-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true # required — renders large CRDs without the 256KB annotation limit
    retry:
      limit: 5
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 5m
