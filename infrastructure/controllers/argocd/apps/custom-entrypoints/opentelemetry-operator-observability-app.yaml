# ServiceMonitor split out of the OTel operator core app: both it and kube-prometheus-stack (CRD owner) are Wave 5,
# so a chart-rendered ServiceMonitor raced the CRD install. Wave 6 keeps this strictly after monitoring.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: opentelemetry-operator-observability
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "6"
    argocd.argoproj.io/manifest-generate-paths: infrastructure/controllers/opentelemetry-operator-observability
  labels:
    app.kubernetes.io/part-of: infrastructure
spec:
  project: infrastructure
  source:
    repoURL: https://github.com/mitchross/talos-argocd-proxmox.git
    targetRevision: main
    path: infrastructure/controllers/opentelemetry-operator-observability
  destination:
    server: https://kubernetes.default.svc
    namespace: opentelemetry
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=false
    - ServerSideApply=true
    - RespectIgnoreDifferences=true
    # Observability-only escape hatch: if monitoring CRDs are momentarily absent
    # this app still syncs without wedging. Core apps must NEVER rely on this.
    - SkipDryRunOnMissingResource=true
    retry:
      limit: 10
      backoff:
        duration: 15s
        factor: 2
        maxDuration: 5m
