# ConfigMaps and feature flags
configs:
  params:
    server.insecure: "true"
    # Performance: increase parallel status checks (default 20, ~1 per 20 apps)
    controller.status.processors: "50"
    # Performance: increase concurrent sync operations (default 10)
    controller.operation.processors: "25"
    # Performance: limit concurrent manifest generations to prevent OOM
    reposerver.parallelism.limit: "5"
    # 1h (not default 24h) bounds repo-server manifest-cache poisoning (argo-cd#26530);
    # hard refresh can't clear a poisoned entry — only restarting argocd-repo-server + argocd-redis does.
    reposerver.repo.cache.expiration: "1h"
    # Performance: increase repo-server timeout for large Helm charts like prometheus-stack
    controller.repo.server.timeout.seconds: "300"
    # Server-side diff must be set HERE (argocd-cmd-params-cm); the argocd-cm key `resource.server-side-diff` is a no-op.
    # Per-app `compare-options: ServerSideDiff=true,IncludeMutationWebhook=true` adds mutating-webhook passthrough.
    controller.diff.server.side: "true"
  cm:
    url: "https://argocd.vanillax.me"
    # Keep --enable-helm; Kustomize's load restrictor stays on (no app may reference files outside its root).
    kustomize.buildOptions: "--enable-helm"
    # 'annotation' alone gets stripped by operator reconciles (cert-manager/ESO/KEDA) → perma-OutOfSync;
    # the app.kubernetes.io/instance label is the durable half. Do not revert to plain 'annotation'.
    application.resourceTrackingMethod: "annotation+label"
    # Default ArgoCD polling interval (180s) with modest jitter
    timeout.reconciliation: "180s"
    timeout.reconciliation.jitter: "60s"
    # Hard refresh invalidates BOTH manifest + cluster live-state caches (soft timeout.reconciliation does not).
    # "0" lets stale live-state persist indefinitely (Synced+Healthy while live lags git); 1h bounds it.
    timeout.hard.reconciliation: "1h"
    # parentRefs: ignore ONLY the defaulted group/kind subfields. Ignoring the whole array or whole entries
    # silently blocks Gateway re-parenting while ArgoCD reports Synced — name/namespace/sectionName must stay diffable.
    resource.customizations.ignoreDifferences.gateway.networking.k8s.io_HTTPRoute: |
      jqPathExpressions:
      - .spec.parentRefs[].group
      - .spec.parentRefs[].kind
      - .spec.rules[].backendRefs[].group
      - .spec.rules[].backendRefs[].kind
      - .spec.rules[].backendRefs[].weight
    resource.customizations.ignoreDifferences.external-secrets.io_ExternalSecret: |
      jqPathExpressions:
      - .metadata.generation
      - .metadata.finalizers
      - .spec.data[].remoteRef.conversionStrategy
      - .spec.data[].remoteRef.decodingStrategy
      - .spec.data[].remoteRef.metadataPolicy
      - .spec.data[].remoteRef.nullBytePolicy
      - .spec.target.deletionPolicy
    resource.customizations.ignoreDifferences._PersistentVolumeClaim: |
      jqPathExpressions:
      - .spec.dataSourceRef
      - .spec.dataSource
      - .spec.volumeName
    # CRDs — K8s API server adds conversion and generation fields
    # NOTE: .status ignored globally by ArgoCD 3.0+ (PR #22230)
    resource.customizations.ignoreDifferences.apiextensions.k8s.io_CustomResourceDefinition: |
      jqPathExpressions:
      - .metadata.generation
      - .spec.conversion
    # OpenTelemetry operator mutates collector after creation
    resource.customizations.ignoreDifferences.opentelemetry.io_OpenTelemetryCollector: |
      jqPathExpressions:
      - .metadata.generation
      - .metadata.annotations
    # Secrets managed by cert-manager/webhooks — caBundle and cert data change
    resource.customizations.ignoreDifferences.all: |
      managedFieldsManagers:
      - kube-controller-manager
      - kube-scheduler
    # Custom health keys are `<API group>_<Kind>`. Modern Argo CD does not assess child Application health
    # automatically — without this, root's sync waves advance before children are Healthy.
    resource.customizations.health.argoproj.io_Application: |
      hs = {}
      hs.status = "Progressing"
      hs.message = ""
      if obj.status ~= nil then
        if obj.status.health ~= nil then
          hs.status = obj.status.health.status
          if obj.status.health.message ~= nil then
            hs.message = obj.status.health.message
          end
        end
      end
      return hs
    # Strimzi Kafka has no built-in Argo health; stays Progressing until Ready/NotReady so KafkaTopic (wave 1) waits for the broker.
    resource.customizations.health.kafka.strimzi.io_Kafka: |
      hs = {}
      hs.status = "Progressing"
      hs.message = "Waiting for Strimzi reconciliation"
      if obj.status ~= nil and obj.status.conditions ~= nil then
        for _, c in ipairs(obj.status.conditions) do
          if c.type == "Ready" and c.status == "True" then
            hs.status = "Healthy"
            hs.message = c.message or "Kafka is ready"
            return hs
          elseif c.type == "NotReady" and c.status == "True" then
            hs.status = "Degraded"
            hs.message = c.message or c.reason or "Kafka is not ready"
            return hs
          end
        end
      end
      return hs
# Argo CD server settings
server:
  # One schedulable worker / one physical failure domain: extra replicas do
  # not add availability. Revisit when workers span multiple hosts.
  replicas: 1
  env:
    - name: ARGOCD_GRPC_MAX_SIZE_MB
      value: "200"
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      additionalLabels:
        release: kube-prometheus-stack
  resources:
    requests:
      cpu: 50m
      memory: 512Mi  # Increased from 256Mi (was below VPA lowerBound)
    limits:
      memory: 1Gi
# ApplicationSet controller settings
applicationSet:
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      additionalLabels:
        release: kube-prometheus-stack
  resources:
    requests:
      cpu: 100m  # 2x VPA target for safety margin
      memory: 128Mi
    limits:
      memory: 512Mi  # Reduced from 1Gi
# Do not enable the redis-ha subchart until ≥3 schedulable nodes span real failure domains (single host today).
redis:
  resources:
    requests:
      cpu: 25m
      memory: 64Mi
    limits:
      memory: 256Mi
# Argo CD controller settings
controller:
  env:
    # Performance: increase K8s API client throughput
    - name: ARGOCD_K8S_CLIENT_QPS
      value: "50"
    - name: ARGOCD_K8S_CLIENT_BURST
      value: "100"
    # Performance: split large app trees across Redis keys
    - name: ARGOCD_APPLICATION_TREE_SHARD_SIZE
      value: "100"
    # ~90% of resources.limits.memory — keep in sync; GC pressure beats OOM-kill during comparison spikes.
    - name: GOMEMLIMIT
      value: "5500MiB"
  metrics:
    enabled: true
    applicationLabels:
      enabled: true
    serviceMonitor:
      enabled: true
      additionalLabels:
        release: kube-prometheus-stack
  resources:
    requests:
      cpu: 2000m
      memory: 4Gi   # Increased from 1536Mi (was below VPA lowerBound)
    limits:
      memory: 6Gi   # Increased from 4Gi to accommodate memory growth
# Argo CD repo-server settings
repoServer:
  # One schedulable worker today; scale with real worker failure domains.
  replicas: 1
  env:
    - name: ARGOCD_EXEC_TIMEOUT
      value: "3m"
    # ~90% of resources.limits.memory — keep in sync; big Helm renders (kube-prometheus-stack) spike the heap.
    - name: GOMEMLIMIT
      value: "3600MiB"
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
      additionalLabels:
        release: kube-prometheus-stack
  resources:
    requests:
      cpu: 1000m   # low steady reservation; bursts to 4000m on big renders
      memory: 768Mi  # Reduced from 1Gi (VPA target only 523Mi)
    limits:
      memory: 4Gi  # Keep at 4Gi (VPA upperBound: 3.95Gi for large Helm charts)
# Dex is disabled. Argo CD is exposed only through the internal Gateway and
# intentionally uses local admin authentication in this single-operator lab.
dex:
  enabled: false
notifications:
  # No triggers, subscriptions, or delivery services are configured. Enable
  # this only when a concrete notification destination is added.
  enabled: false
# The chart's redis-secret-init PreSync hook Job races Argo's operationState tracking on upgrades and wedges
# the sync; the argocd-redis Secret already exists, so the bootstrapper stays off.
redisSecretInit:
  enabled: false
