# Synced after kube-prometheus-stack (custom-entrypoints/keda-observability-app.yaml) so monitoring.coreos.com CRDs exist.
# Bootstrap KEDA (infrastructure/controllers/keda) must keep rendering ZERO monitoring CRs.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: keda
resources:
  - servicemonitors.yaml
