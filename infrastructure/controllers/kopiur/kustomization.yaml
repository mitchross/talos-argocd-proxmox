apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# kopiur config only (the operator is core-dependencies/kopiur-operator-app.yaml); synced at Wave 3 after the CRDs land at Wave 2.
resources:
  - namespace.yaml
  - externalsecret.yaml
  - clusterrepository.yaml
  - volumesnapshotclass.yaml # cluster default snapclass kopiur backups need (moved from retired volsync)
