# The GPU Operator does not manage power limits; this reapplies `nvidia-smi -pl` every 5 min (survives driver reloads).
# Only gpu-worker=true nodes get it — CPU-only workers must never receive the 3090's cap.
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: nvidia-powerlimit
  namespace: gpu-operator
  labels:
    app.kubernetes.io/name: nvidia-powerlimit
    app.kubernetes.io/component: power-management
    app.kubernetes.io/part-of: gpu-infrastructure
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: nvidia-powerlimit
  template:
    metadata:
      labels:
        app.kubernetes.io/name: nvidia-powerlimit
        app.kubernetes.io/component: power-management
    spec:
      # CDI-injected nvidia-smi is available when runtimeClassName is "nvidia"
      runtimeClassName: nvidia
      # Only schedule on the established RTX worker.
      nodeSelector:
        nvidia.com/gpu.present: "true"
        gpu-worker: "true"
      # Match the rest of the GPU stack's tolerations so this DS is
      # allowed onto tainted GPU nodes.
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        - key: gpu
          operator: Equal
          value: "true"
          effect: NoSchedule
      hostPID: true
      containers:
        - name: nvidia-powerlimit
          # renovate: ignore
          # nvcr.io is byte-identical to docker.io/nvidia/cuda and dodges Docker Hub rate limits.
          image: nvcr.io/nvidia/cuda:12.8.1-base-ubuntu22.04
          command:
            - /bin/bash
            - -c
            - /opt/repo-scripts/reconcile-power-limit.sh
          volumeMounts:
            - name: repo-scripts
              mountPath: /opt/repo-scripts
              readOnly: true
          securityContext:
            # Required for nvidia-smi -pl; the driver rejects the ioctl
            # from an unprivileged user namespace.
            privileged: true
          env:
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "utility"
            # 200W is a HOUSE-CIRCUIT constraint, not an efficiency choice (290W flickered the basement lights).
            # Do not raise toward the 290W efficiency knee without confirming the circuit.
            - name: POWER_LIMIT_WATTS
              value: "220"
            - name: REAPPLY_SECONDS
              value: "300"
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 128Mi
      volumes:
        - name: repo-scripts
          configMap:
            name: nvidia-powerlimit-scripts
            defaultMode: 0555
