manager:
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 256Mi
  # Keep optional ServiceMonitors separate so missing monitoring CRDs cannot block the Collector app.
  serviceMonitor:
    enabled: false

admissionWebhooks:
  certManager:
    enabled: true
