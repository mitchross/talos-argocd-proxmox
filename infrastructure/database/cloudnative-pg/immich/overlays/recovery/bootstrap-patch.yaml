apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: immich-database
spec:
  bootstrap:
    recovery:
      source: immich-recovery-source
      recoveryTarget:
        targetTime: "2026-04-16T23:59:59Z"
  externalClusters:
    - name: immich-recovery-source
      barmanObjectStore:
        # Prior lineage with pre-nuke data.
        serverName: immich-database-v6
        destinationPath: s3://postgres-backups/cnpg/immich
        endpointURL: http://192.168.10.133:30293
        s3Credentials:
          accessKeyId:
            name: cnpg-s3-credentials
            key: AWS_ACCESS_KEY_ID
          secretAccessKey:
            name: cnpg-s3-credentials
            key: AWS_SECRET_ACCESS_KEY
        wal:
          compression: gzip
