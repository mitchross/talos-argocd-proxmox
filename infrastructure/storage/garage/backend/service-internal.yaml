apiVersion: v1
kind: Service
metadata:
  name: garage-internal
  namespace: garage
spec:
  clusterIP: None  # Headless service for StatefulSet
  selector:
    app: garage
  ports:
  - name: rpc
    port: 3901
    targetPort: 3901
    protocol: TCP
