apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - rbac.yaml
  - init-layout-job.yaml
  - httproute.yaml

# Note: garage-app.yaml is deployed separately as a standalone ArgoCD Application
# It's not included here to avoid nested Application management
