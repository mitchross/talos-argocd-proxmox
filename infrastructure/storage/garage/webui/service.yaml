apiVersion: v1
kind: Service
metadata:
  name: garage-webui
  namespace: garage
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: ClusterIP
  selector:
    app: garage-webui
  ports:
  - name: http
    port: 3909
    targetPort: http
    protocol: TCP
