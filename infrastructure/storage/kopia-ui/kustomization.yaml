---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kopia-ui
resources:
  - namespace.yaml
  - externalsecret.yaml
  - deployment.yaml
  - httproute.yaml
# repository.config configMapGenerator removed 2026-06-27 — the kopiur S3 repo
# connection is now established at runtime by the deployment's init container
# (creds from ESO), so there's no static config file to mount.
