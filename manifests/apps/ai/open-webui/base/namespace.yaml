apiVersion: v1
kind: Namespace
metadata:
  name: open-webui
  labels:
    volsync.backube/privileged-movers: "true"
    # pvc-plumber v4.0.1 namespace write-gate (2026-05-30): opts open-webui in so
    # the operator may create/repair RS/RD for fused PVCs (storage).
    pvc-plumber.io/managed-namespace: "true"
