apiVersion: v1
kind: Namespace
metadata:
  name: perplexica
  labels:
    volsync.backube/privileged-movers: 'true'
    # pvc-plumber v4.0.1 namespace write-gate (2026-05-30): opts perplexica in so
    # the operator may create/repair RS/RD for fused PVCs (perplexica-data).
    pvc-plumber.io/managed-namespace: 'true'
