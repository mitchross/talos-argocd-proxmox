apiVersion: v1
kind: Namespace
metadata:
  name: swarmui
  labels:
    volsync.backube/privileged-movers: 'true'
    # pvc-plumber v4.0.1 namespace write-gate (2026-05-30): opts swarmui in so the
    # operator may create/repair RS/RD for fused PVCs (swarmui-data, -output).
    pvc-plumber.io/managed-namespace: 'true'
