apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qdrant-data
  namespace: project-nomad
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=false
    argocd.argoproj.io/sync-options: ServerSideApply=false
  labels:
    app: project-nomad
    type: qdrant
    restore-policy: "strict"
    # pvc-plumber v4.0.1 DRY handoff (2026-05-30): operator-managed at
    # tier=daily; ns project-nomad carries managed-namespace=true. Mover 568.
    pvc-plumber.io/enabled: "true"
    pvc-plumber.io/manage-volsync: "true"
    pvc-plumber.io/tier: "daily"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: vanillax-local-rwo
  # Static dataSourceRef → ReplicationDestination/qdrant-data-dst. No-op
  # while the PVC is already Bound. RS/RD are operator-managed by
  # pvc-plumber v4.0.1 (DRY handoff, 2026-05-30): the operator creates and
  # repairs ReplicationSource/qdrant-data + ReplicationDestination/
  # qdrant-data-dst as managed-by=pvc-plumber via its RS/RD watch.
  dataSourceRef:
    apiGroup: volsync.backube
    kind: ReplicationDestination
    name: qdrant-data-dst
