apiVersion: v1
kind: Service
metadata:
  name: embeddings
  namespace: project-nomad
  labels:
    app: embeddings
spec:
  selector:
    app: embeddings
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
