# SCALED TO ZERO: the node-agent eBPF profiler re-reads every container binary each cycle (~1 GB/s disk reads, IO-stalled the Proxmox host).
# Operator 1.9.6 treats replicas: 0 as unset — only the unschedulable nodeSelector actually zeroes the components.
apiVersion: coroot.com/v1
kind: Coroot
metadata:
  name: coroot
  namespace: coroot
spec:
  communityEdition: {}
  replicas: 0
  nodeSelector:
    coroot.vanillax.dev/enabled: "true"
  metricsRefreshInterval: 30s
  storage:
    size: 10Gi
    className: longhorn
    reclaimPolicy: Delete
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      memory: 1Gi
  prometheus:
    retention: 2d
    nodeSelector:
      coroot.vanillax.dev/enabled: "true"
    storage:
      size: 10Gi
      className: longhorn
      reclaimPolicy: Delete
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        memory: 1Gi
  clickhouse:
    replicas: 0
    nodeSelector:
      coroot.vanillax.dev/enabled: "true"
    storage:
      size: 10Gi
      className: longhorn
      reclaimPolicy: Delete
    resources:
      requests:
        cpu: 200m
        memory: 768Mi
      limits:
        memory: 2Gi
    keeper:
      replicas: 0
      nodeSelector:
        coroot.vanillax.dev/enabled: "true"
      storage:
        size: 2Gi
        className: longhorn
        reclaimPolicy: Delete
      resources:
        requests:
          cpu: 50m
          memory: 128Mi
        limits:
          memory: 256Mi
  nodeAgent:
    # The read storm. eBPF profiling re-reads container binaries to symbolize
    # stacks; the tracer and log collector are off for the same reason.
    nodeSelector:
      coroot.vanillax.dev/enabled: "true"
    ebpfProfiler:
      enabled: false
    ebpfTracer:
      enabled: false
    logCollector:
      collectLogEntries: false
      collectLogBasedMetrics: false
    resources:
      requests:
        cpu: 100m
        memory: 200Mi
      limits:
        memory: 1Gi
    tolerations:
      - operator: Exists
  clusterAgent:
    nodeSelector:
      coroot.vanillax.dev/enabled: "true"
    resources:
      requests:
        cpu: 50m
        memory: 128Mi
      limits:
        memory: 512Mi
  logsTTL: 3d
  tracesTTL: 3d
  profilesTTL: 3d
  service:
    type: ClusterIP
    port: 8080
