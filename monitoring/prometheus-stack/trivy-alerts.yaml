# Alerts only when a single ACTIVE image accumulates many criticals (any-critical would storm); ReplicaSet reports join kube-state-metrics to drop scaled-to-zero revisions.
# trivy_image_exposedsecrets is deliberately not alerted: current hits are Ruby gem test fixtures — add a .trivyignore first.
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: trivy-alerts
  namespace: prometheus-stack
  labels:
    app.kubernetes.io/name: kube-prometheus-stack
    app.kubernetes.io/instance: kube-prometheus-stack
    app.kubernetes.io/managed-by: Helm
    prometheus: kube-prometheus-stack-prometheus
    role: alert-rules
spec:
  groups:
    - name: trivy.vulnerabilities
      rules:
        - alert: TrivyImageHighCriticalCount
          # Tunable threshold; non-ReplicaSet reports pass through, ReplicaSet reports need a live replica.
          expr: >-
            max by (namespace, image_repository, image_tag) (
              trivy_image_vulnerabilities{
                severity="Critical",
                resource_kind!="ReplicaSet"
              }
              or
              (
                trivy_image_vulnerabilities{
                  severity="Critical",
                  resource_kind="ReplicaSet"
                }
                and on(namespace, resource_name)
                label_replace(
                  kube_replicaset_status_replicas > 0,
                  "resource_name",
                  "$1",
                  "replicaset",
                  "(.+)"
                )
              )
            ) > 15
          for: 30m
          labels:
            severity: warning
            component: trivy-operator
            category: security
          annotations:
            summary: "{{ $labels.image_repository }}:{{ $labels.image_tag }} has {{ $value }} critical CVEs"
            description: >-
              Image {{ $labels.image_repository }}:{{ $labels.image_tag }} in
              namespace {{ $labels.namespace }} has {{ $value }} fixable CRITICAL
              vulnerabilities (ignoreUnfixed is on, so all are actionable). Bump
              the image tag or rebuild it on a newer base image, then re-scan.
