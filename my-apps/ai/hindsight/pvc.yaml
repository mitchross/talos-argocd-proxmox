apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hindsight-db-pvc
  namespace: hindsight
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
