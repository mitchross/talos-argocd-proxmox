apiVersion: v1
kind: Service
metadata:
  name: langfuse-postgres-metrics
  namespace: langfuse
  labels:
    app.kubernetes.io/name: langfuse-postgres
    app.kubernetes.io/component: postgres-metrics
spec:
  selector:
    app: langfuse-postgres
  ports:
  - name: metrics
    port: 9187
    targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: langfuse-postgres
  namespace: langfuse
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: langfuse-postgres
      app.kubernetes.io/component: postgres-metrics
  endpoints:
  - port: metrics
    interval: 30s
    scrapeTimeout: 10s
