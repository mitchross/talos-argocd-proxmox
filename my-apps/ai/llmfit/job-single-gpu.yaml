apiVersion: batch/v1
kind: Job
metadata:
  name: llmfit-single-gpu
  namespace: llmfit
  labels:
    app: llmfit-single
spec:
  # Set suspend: true if you want to pause this job next time so it doesn't run automatically
  suspend: false
  template:
    metadata:
      labels:
        app: llmfit-single
    spec:
      restartPolicy: Never
      runtimeClassName: nvidia
      priorityClassName: gpu-workload-high
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      - key: "gpu"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"
      containers:
      - name: llmfit
        image: ghcr.io/alexsjones/llmfit:latest@sha256:40135558ba93511afc8dca184f0696cd8b456537157a6fb7837389b6f2ce84b3
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            memory: 16Gi
            nvidia.com/gpu: "1"
          requests:
            cpu: "1000m"
            memory: 8Gi
            nvidia.com/gpu: "1"
      nodeSelector:
        feature.node.kubernetes.io/pci-0300_10de.present: "true"
        gpu-worker: "true"
