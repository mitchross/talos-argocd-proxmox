apiVersion: v1
kind: Service
metadata:
  name: surfsense-postgres-metrics
  namespace: surfsense
  labels:
    app.kubernetes.io/name: surfsense-postgres
    app.kubernetes.io/component: postgres-metrics
spec:
  selector:
    app: surfsense-postgres
  ports:
  - name: metrics
    port: 9187
    targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: surfsense-postgres
  namespace: surfsense
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: surfsense-postgres
      app.kubernetes.io/component: postgres-metrics
  endpoints:
  - port: metrics
    interval: 30s
    scrapeTimeout: 10s
