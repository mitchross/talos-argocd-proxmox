apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: surfsense
  labels:
    app: surfsense-postgres
spec:
  selector:
    app: surfsense-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
