apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: surfsense
  labels:
    app: surfsense-redis
spec:
  selector:
    app: surfsense-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
