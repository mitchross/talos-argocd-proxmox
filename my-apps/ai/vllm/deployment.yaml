apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-server
  namespace: vllm
  labels:
    app: vllm-server
spec:
  # vLLM is the active in-cluster LLM backend (TP=2 across both 3090s).
  # llama-cpp is scaled to 0 (frees GPU0); comfyui already 0 (GPU1 idle).
  # To revert: set this to 0 and llama-cpp-server back to 1 (one commit).
  replicas: 1
  strategy:
    type: Recreate            # whole-card GPU workload; never two pods on the cards at once
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: vllm-server
  template:
    metadata:
      labels:
        app: vllm-server
    spec:
      restartPolicy: Always
      runtimeClassName: nvidia
      priorityClassName: gpu-workload-high
      terminationGracePeriodSeconds: 120
      nodeSelector:
        feature.node.kubernetes.io/pci-0300_10de.present: "true"
      tolerations:
        - key: "nvidia.com/gpu"
          operator: "Exists"
          effect: "NoSchedule"
      securityContext:
        fsGroup: 10001
        fsGroupChangePolicy: "OnRootMismatch"
      containers:
        - name: vllm-server
          # TODO: pin to a tag that supports the Qwen3.6 / Qwen3-VL architecture.
          # `latest` is a placeholder — Renovate/this repo prefer pinned tags.
          image: vllm/vllm-openai:latest
          imagePullPolicy: IfNotPresent
          # The image entrypoint runs `vllm serve`; the first arg is the positional model_tag.
          args:
            - "/models/Qwen3.6-27B-AWQ-BF16-INT4"     # default model (higher quality; fits TP=2 / 48GB)
            # served-model-name is a LIST (nargs+): advertise the new canonical id AND every llama-cpp
            # model id the existing tools already send — so repointing those tools is URL-only, no model
            # field changes, nothing 404s. The next token MUST be a flag (--tensor-parallel-size) to end the list.
            - "--served-model-name"
            - "qwen3.6-27b"                                    # canonical
            - "default"                                        # llama-cpp daily-driver alias
            - "daily"
            - "qwen3.6-nothink - qwen3.6-35b-a3b-nothink"      # what open-webui/k8sgpt/n8n send
            - "qwen3.6 - qwen3.6-35b-a3b"                      # what comfyui/perplexica send
            - "qwen3.6-longctx - qwen3.6-35b-a3b-longctx"      # perplexica research (note: capped to --max-model-len)
            - "--tensor-parallel-size"
            - "2"                                      # pool both 3090s (48GB). Needs 2 WHOLE cards.
            - "--distributed-executor-backend"
            - "mp"                                     # single-node multiprocessing; no Ray
            - "--gpu-memory-utilization"
            - "0.90"
            - "--max-model-len"
            - "65536"                                  # 48GB pooled affords large KV; tune vs club-3090 CLIFFS.md
            - "--max-num-seqs"
            - "16"
            - "--limit-mm-per-prompt"
            - '{"image": 4}'                           # vision-capable; current vLLM requires JSON, not image=4
            - "--host"
            - "0.0.0.0"
            - "--port"
            - "8000"
          env:
            # GPU selection is handled by the device plugin + CDI (whole-card, time-slicing OFF).
            # Do NOT set NVIDIA_VISIBLE_DEVICES / CUDA_VISIBLE_DEVICES — they override CDI injection.
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "compute,utility"
            - name: HF_HUB_OFFLINE          # models are local on NFS; never call the Hub at runtime
              value: "1"
            - name: TRANSFORMERS_OFFLINE
              value: "1"
            - name: VLLM_NO_USAGE_STATS     # no phone-home
              value: "1"
          ports:
            - name: http
              containerPort: 8000
              protocol: TCP
          startupProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 360        # TP load of a 27B can take minutes
          livenessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: http }
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            # GPU node has 16 vCPU — keep request schedulable and limit within the node.
            requests:
              nvidia.com/gpu: "2"
              cpu: "4"
              memory: 24Gi
            limits:
              nvidia.com/gpu: "2"
              cpu: "14"          # leave ~2 vCPU for kubelet/cilium/device-plugin/otel
              memory: 64Gi
          volumeMounts:
            - name: models
              mountPath: /models
              readOnly: true
            - name: dshm
              mountPath: /dev/shm        # TP workers IPC over shared memory — size it generously
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: vllm-models-pvc
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
