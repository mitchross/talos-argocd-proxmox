# DVWA is intentionally vulnerable — network-policy/pentest testing only, never left running.
# Uncomment deployment/service/httproute to enable; commented out is the default.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dvwa

resources:
  - ns.yaml
  # - deployment.yaml
  # - service.yaml
  # - httproute.yaml
