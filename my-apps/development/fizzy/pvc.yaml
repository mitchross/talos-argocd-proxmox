apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fizzy-data
  namespace: fizzy
  labels:
    app.kubernetes.io/name: fizzy
    app.kubernetes.io/component: kanban
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
