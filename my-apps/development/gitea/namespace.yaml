apiVersion: v1
kind: Namespace
metadata:
  name: gitea
  labels:
    volsync.backube/privileged-movers: 'true'
    # pvc-plumber v4.0.1 namespace write-gate (2026-05-31): opts gitea in so the
    # operator may create/repair RS/RD for the fused gitea-shared-storage PVC.
    pvc-plumber.io/managed-namespace: 'true'
