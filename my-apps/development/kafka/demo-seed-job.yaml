apiVersion: batch/v1
kind: Job
metadata:
  name: kafka-demo-seed
  namespace: kafka
  annotations:
    # Run only after Kafka (wave 0) and dev-events (wave 1) are Healthy.
    # BeforeHookCreation makes this immutable Job safe on later Git changes.
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "2"
spec:
  activeDeadlineSeconds: 180
  backoffLimit: 2
  template:
    metadata:
      labels:
        app.kubernetes.io/component: demo-seed
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 0
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: seed-and-verify
          # Match the broker's Kafka CLI version. Digest pin keeps the demo
          # reproducible while the readable tag documents operator/Kafka ABI.
          image: quay.io/strimzi/kafka:1.2.0-kafka-4.3.0@sha256:e90a1a74af4226f3ca4d1ebef3ab13bdb09754ae17ca4c1444f7fcbb0ca8ea9a
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - /demo/seed-and-verify.sh
          env:
            - name: KAFKA_HEAP_OPTS
              value: -Xms64m -Xmx192m
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: demo
              mountPath: /demo
              readOnly: true
      volumes:
        - name: tmp
          emptyDir: {}
        - name: demo
          configMap:
            name: kafka-demo
