apiVersion: v1
kind: Service
metadata:
  name: mcp-grafana
  namespace: mcp-grafana
spec:
  selector:
    app: mcp-grafana
  ports:
    # Named "http" so the HTTPRoute can bind to it — unnamed ports fail silently.
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
