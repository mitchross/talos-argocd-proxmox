<clickhouse>
    <tcp_port>9000</tcp_port>

    <remote_servers>
        <posthog>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog>
        <posthog_single_shard>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_single_shard>
        <posthog_migrations>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_migrations>
        <posthog_writable>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_writable>
        <posthog_primary_replica>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </posthog_primary_replica>
        <!--
          ai_events — newer PostHog (image sha 9b518de) expects this cluster
          when running `migrate_clickhouse`. It's used by the AI/LLM event
          ingestion path which assumes a separate "satellite" ClickHouse
          cluster. On single-node installs we point it at a headless Service
          so ClickHouse resolves the pod IP and marks the replica local.
          Without this cluster, every
          migrate_clickhouse run fails with:
            DB::Exception: Requested cluster 'ai_events' not found (code 701)
          which cascades into the entire posthog-migrate PostSync hook
          hitting backoffLimit and Argo wedging the app sync.
        -->
        <ai_events>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </ai_events>
        <!--
          Satellite clusters — PostHog's `CLICKHOUSE_SATELLITE_CLUSTERS`
          defaults to `"ai_events,aux,ops,sessions"` (see
          posthog/settings/data_stores.py). All four must exist or
          `migrate_clickhouse` fails with
          `DB::Exception: Requested cluster '<name>' not found (code 701)`
          and cascades into the PostSync hook wedging Argo. On a single-
          node install we point every satellite at the same headless Service;
          queries resolve transparently.
        -->
        <aux>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </aux>
        <ops>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </ops>
        <sessions>
            <shard>
                <replica>
                    <host>clickhouse.clickhouse-headless.posthog.svc.cluster.local</host>
                    <port>9000</port>
                </replica>
            </shard>
        </sessions>
    </remote_servers>

    <!--
      named_collections — MUST mirror upstream
      PostHog/posthog:docker/clickhouse/config.d/default.xml (all alias
      $KAFKA_HOSTS; Kafka-engine DDL in migrations references them by name).
      A collection missing here hard-fails migrate_clickhouse — diff upstream
      before every monolith bump (UPGRADE.md step 1).
    -->
    <named_collections>
        <msk_cluster>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </msk_cluster>
        <warpstream_ingestion>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_ingestion>
        <warpstream_calculated_events>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_calculated_events>
        <warpstream_replay>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_replay>
        <warpstream_shared>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_shared>
        <warpstream_cyclotron>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_cyclotron>
        <warpstream_logs>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_logs>
        <warpstream_traces>
            <kafka_broker_list from_env="KAFKA_HOSTS"/>
        </warpstream_traces>
    </named_collections>

    <macros>
        <shard>01</shard>
        <replica>ch1</replica>
        <hostClusterType>online</hostClusterType>
        <hostClusterRole>data</hostClusterRole>
    </macros>
</clickhouse>
