# maxUnavailable, not minAvailable: 1 — a single-replica minAvailable PDB blocks Talos drains.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tile-server
  namespace: radar-ng
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: tile-server
