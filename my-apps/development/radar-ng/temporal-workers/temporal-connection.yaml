# Self-hosted, no mTLS. To add it, create a Secret with tls.crt/tls.key/ca.crt and set spec.mutualTLSSecretRef.
apiVersion: temporal.io/v1alpha1
kind: Connection
metadata:
  name: cluster-temporal
  namespace: radar-ng
spec:
  hostPort: "temporal-frontend.temporal.svc.cluster.local:7233"
