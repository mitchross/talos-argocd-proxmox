apiVersion: apps/v1
kind: Deployment
metadata:
  name: vert
  namespace: vert
  labels:
    app.kubernetes.io/name: vert
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vert
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vert
    spec:
      containers:
        - name: vert
          image: ghcr.io/vert-sh/vert:latest@sha256:d5f4bf9ae3fa958a7670deca744049a5d7864d9165855cd05b01348694c7c510
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          env:
            - name: PUB_ENV
              value: "production"
            - name: PUB_HOSTNAME
              value: "vert.vanillax.me"
            # Optional: Configure vertd URL for video conversion
            # - name: PUB_VERTD_URL
            #   value: "http://vertd.development.svc.cluster.local:24153"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 512Mi
