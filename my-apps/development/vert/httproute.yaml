apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vert-route
  namespace: vert
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: gateway-external
      namespace: gateway
  hostnames:
    - "vert.vanillax.me"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: vert
          port: 80
          weight: 1
