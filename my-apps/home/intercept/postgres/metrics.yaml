apiVersion: v1
kind: Service
metadata:
  name: intercept-postgres-metrics
  namespace: intercept
  labels:
    app.kubernetes.io/name: intercept-postgres
    app.kubernetes.io/component: postgres-metrics
spec:
  selector:
    app.kubernetes.io/name: intercept-postgres
  ports:
  - name: metrics
    port: 9187
    targetPort: metrics
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: intercept-postgres
  namespace: intercept
  labels:
    release: prometheus-stack
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: intercept-postgres
      app.kubernetes.io/component: postgres-metrics
  endpoints:
  - port: metrics
    interval: 30s
    scrapeTimeout: 10s
