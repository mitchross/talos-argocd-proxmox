# Minimal NetBox values; using bundled PostgreSQL and Valkey for simplicity.

allowedHosts:
- netbox.vanillax.me

postgresql:
  enabled: true
  auth:
    username: netbox
    database: netbox
    password: "0sMMxAr+CtY03x5Ji9WxsuuLjnRZ8urH194x+zd3Fzg="
    # Optional but recommended to set as well
    postgresPassword: "Y3RPsv/oCRVAuzFrI979VKiizSNl+eLy/m1CyAG41Ys="
  primary:
    persistence:
      enabled: true
      size: 10Gi
      storageClass: longhorn

valkey:
  enabled: true
  master:
    persistence:
      enabled: true
      size: 8Gi
      storageClass: longhorn
    configuration: |
      # Disable AOF to avoid I/O errors
      appendonly no
      # Enable RDB persistence instead
      save 900 1
      save 300 10
      save 60 10000

persistence:
  enabled: true
  storageClass: longhorn
  size: 10Gi

worker:
  enabled: true

metrics:
  enabled: false
  serviceMonitor:
    enabled: false
