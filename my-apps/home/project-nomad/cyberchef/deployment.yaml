apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyberchef
  namespace: project-nomad
  labels:
    app: project-nomad-cyberchef
spec:
  replicas: 1
  selector:
    matchLabels:
      app: project-nomad-cyberchef
  template:
    metadata:
      labels:
        app: project-nomad-cyberchef
      # No OTEL injection: static SPA on nginx, inject-nodejs only gated pod start for zero telemetry.
    spec:
      containers:
        - name: cyberchef
          image: ghcr.io/gchq/cyberchef:11.4.0
          ports:
            # gchq/cyberchef is nginx-unprivileged: it listens on 8080 and
            # cannot bind 80. containerPort 80 made the gateway 503 silently.
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 15m
              memory: 100Mi
            limits:
              memory: 256Mi
