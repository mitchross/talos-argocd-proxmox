apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flatnotes-data
  namespace: project-nomad
  labels:
    app: project-nomad
    type: flatnotes
    backup: "daily"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
