apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flatnotes-data
  namespace: project-nomad
  labels:
    app: project-nomad
    type: flatnotes
    backup: "daily"
    restore-policy: "strict"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  # Static dataSourceRef → ReplicationDestination/flatnotes-data-dst.
  # On PVC re-creation (DR / namespace recreate), VolSync's volume
  # populator reads the latest snapshot from the shared kopia repo
  # volsync-kopia-repository and binds this PVC with restored data. No-op
  # while the PVC is already Bound. VolSync ReplicationSource/
  # ReplicationDestination resources are defined below in this file.
  dataSourceRef:
    apiGroup: volsync.backube
    kind: ReplicationDestination
    name: flatnotes-data-dst

---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: flatnotes-data
  namespace: project-nomad
  labels:
    app.kubernetes.io/managed-by: argocd
    volsync.backup/pvc: flatnotes-data
spec:
  sourcePVC: flatnotes-data
  trigger:
    schedule: "2 2 * * *"
  kopia:
    repository: volsync-kopia-repository
    username: flatnotes-data
    hostname: project-nomad
    compression: zstd-fastest
    parallelism: 2
    retain:
      hourly: 24
      daily: 7
      weekly: 4
      monthly: 2
    copyMethod: Snapshot
    storageClassName: longhorn
    volumeSnapshotClassName: longhorn-snapclass
    cacheCapacity: 2Gi
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationDestination
metadata:
  name: flatnotes-data-dst
  namespace: project-nomad
  labels:
    app.kubernetes.io/managed-by: argocd
    volsync.backup/pvc: flatnotes-data
spec:
  trigger:
    manual: restore-once
  kopia:
    repository: volsync-kopia-repository
    username: flatnotes-data
    hostname: project-nomad
    sourceIdentity:
      sourceName: flatnotes-data
      sourceNamespace: project-nomad
      sourcePVCName: flatnotes-data
    copyMethod: Snapshot
    storageClassName: longhorn
    volumeSnapshotClassName: longhorn-snapclass
    cacheCapacity: 2Gi
    accessModes:
      - ReadWriteOnce
    capacity: 5Gi
    moverSecurityContext:
      runAsUser: 568
      runAsGroup: 568
      fsGroup: 568
