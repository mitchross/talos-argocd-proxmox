apiVersion: apps/v1
kind: Deployment
metadata:
  name: protomaps
  namespace: project-nomad
  labels:
    app: project-nomad-protomaps
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: project-nomad-protomaps
  template:
    metadata:
      labels:
        app: project-nomad-protomaps
      annotations:
        instrumentation.opentelemetry.io/inject-go: "opentelemetry/default"
    spec:
      containers:
        - name: protomaps
          image: ghcr.io/protomaps/go-pmtiles:latest@sha256:06574f01f55a78f78f887bc7ebf729a5c093c0d6e17d9876300cfcb0758b59d3
          args: ["serve", "/data"]
          ports:
            - containerPort: 8080
              name: http
          resources:
            requests:
              cpu: 15m
              memory: 128Mi
            limits:
              memory: 256Mi
          volumeMounts:
            - name: protomaps-data
              mountPath: /data
      volumes:
        - name: protomaps-data
          persistentVolumeClaim:
            claimName: protomaps-data
