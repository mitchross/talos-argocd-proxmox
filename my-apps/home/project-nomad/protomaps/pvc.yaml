apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: protomaps-data
  namespace: project-nomad
  labels:
    app: project-nomad
    type: protomaps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
