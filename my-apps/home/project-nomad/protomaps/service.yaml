apiVersion: v1
kind: Service
metadata:
  name: protomaps
  namespace: project-nomad
  labels:
    app: project-nomad-protomaps
spec:
  selector:
    app: project-nomad-protomaps
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
