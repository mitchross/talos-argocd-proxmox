apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: project-zomboid

resources:
  - namespace.yaml
  - pvc.yaml
  - externalsecret.yaml
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: zomboid-server-config
    files:
      - vanillax.ini
      - vanillax_SandboxVars.lua
