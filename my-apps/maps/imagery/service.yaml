apiVersion: v1
kind: Service
metadata:
  name: imagery
  namespace: imagery
  labels:
    app.kubernetes.io/name: imagery
spec:
  selector:
    app.kubernetes.io/name: imagery
  ports:
    - name: http  # named port — HTTPRoute fails silently without it
      port: 8080
      targetPort: http
