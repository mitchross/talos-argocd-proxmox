"""
This file is kept for historical reasons and has been superseded by
`externalsecret-merged.yaml` which consolidates the two ExternalSecrets
into a single ExternalSecret that populates `garage-webui-secret` with both
`API_ADMIN_KEY` and `rpcSecret` from the 1Password item `s3-garage`.

Do not use this file. The active manifest is `externalsecret-merged.yaml`.
"""
  refreshInterval: "1h"
