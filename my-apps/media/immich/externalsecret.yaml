---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: immich-db-user-secret
  namespace: immich
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: 1password
  target:
    name: immich-pguser-immich
    creationPolicy: Merge
    template:
      metadata:
        labels:
          postgres-operator.crunchydata.com/cluster: immich
          postgres-operator.crunchydata.com/pguser: immich
          postgres-operator.crunchydata.com/role: pguser
      data:
        user: "{{ .immich_db_username }}"
        password: "{{ .immich_db_password }}"
        dbname: "immich"
        host: "immich-pgbouncer.immich.svc.cluster.local"
        port: "5432"
        uri: "postgresql://{{ .immich_db_username }}:{{ .immich_db_password }}@immich-pgbouncer.immich.svc.cluster.local:5432/immich?sslmode=disable"
  dataFrom:
  - extract:
      key: postgres-secrets
