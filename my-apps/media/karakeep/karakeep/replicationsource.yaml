apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: karakeep-data-backup
  namespace: karakeep
spec:
  sourcePVC: data-pvc
  trigger:
    schedule: "0 * * * *"  # Hourly (critical)
  restic:
    pruneIntervalDays: 7
    repository: karakeep-volsync-secret
    retain:
      hourly: 24
      daily: 7
    copyMethod: Snapshot
    storageClassName: longhorn
    cacheStorageClassName: longhorn
