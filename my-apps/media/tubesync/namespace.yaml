apiVersion: v1
kind: Namespace
metadata:
  name: tubesync
  labels:
    volsync.backube/privileged-movers: 'true'
    # pvc-plumber v4.0.1 namespace write-gate: opts this namespace in so the
    # operator may create/repair RS/RD for fused PVCs here (config-pvc).
    pvc-plumber.io/managed-namespace: 'true'
