apiVersion: v1
kind: Service
metadata:
  name: sticktock-backend-service
  namespace: sticktock
  labels:
    app.kubernetes.io/name: sticktock-backend
    app.kubernetes.io/component: backend
spec:
  selector:
    app: sticktock-backend
  ports:
    - name: http
      protocol: TCP
      port: 2000
      targetPort: 2000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: sticktock-frontend-service
  namespace: sticktock
  labels:
    app.kubernetes.io/name: sticktock-frontend
    app.kubernetes.io/component: frontend
spec:
  selector:
    app: sticktock-frontend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP