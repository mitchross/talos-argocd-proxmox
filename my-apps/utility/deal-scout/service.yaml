apiVersion: v1
kind: Service
metadata:
  name: deal-scout
  namespace: deal-scout
  labels:
    app: deal-scout
spec:
  selector:
    app: deal-scout
  ports:
    - name: http # CRITICAL — HTTPRoute matches by port name; fails silently without it
      port: 8000
      targetPort: 8000
