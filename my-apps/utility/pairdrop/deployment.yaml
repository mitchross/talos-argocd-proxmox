apiVersion: apps/v1
kind: Deployment
metadata:
  name: pairdrop
  namespace: pairdrop
  labels:
    app.kubernetes.io/name: pairdrop
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: pairdrop
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pairdrop
      annotations:
        instrumentation.opentelemetry.io/inject-nodejs: "opentelemetry/default"
    spec:
      containers:
      - name: pairdrop
        image: ghcr.io/linuxserver/pairdrop:latest@sha256:9222fcd6de4039613f77de69aa946f3446263056419f8ac82838836b74c37d03
        imagePullPolicy: IfNotPresent
        env:
        - name: TZ
          value: America/Detroit
        - name: PUID
          value: "1000"
        - name: PGID
          value: "1000"
        # Rate limiting settings
        - name: RATE_LIMIT
          value: "false"
        # WebSocket keep-alive
        - name: WS_FALLBACK
          value: "false"
        ports:
        - name: http
          containerPort: 3000
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            memory: 128Mi
        livenessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
