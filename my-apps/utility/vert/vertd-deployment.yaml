apiVersion: apps/v1
kind: Deployment
metadata:
  name: vertd
  namespace: vert
  labels:
    app.kubernetes.io/name: vertd
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: vertd
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vertd
    spec:
      containers:
        - name: vertd
          image: ghcr.io/vert-sh/vertd:latest@sha256:584dbae066a503ea01be952cc80b1cc6fbcf5f4b66d6fac41daa6b600c7af580
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 24153
          env:
            # - name: VERTD_FORCE_GPU
            #   value: nvidia # or amd, intel
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
          volumeMounts:
            - name: dev-dri
              mountPath: /dev/dri
              readOnly: true
      volumes:
        - name: dev-dri
          hostPath:
            path: /dev/dri
            type: DirectoryOrCreate
      # runtimeClassName: nvidia
      # tolerations:
      #   - key: nvidia.com/gpu
      #     operator: Exists
      #     effect: NoSchedule
      # nodeSelector:
      #   feature.node.kubernetes.io/pci-0300_10de.present: "true"
