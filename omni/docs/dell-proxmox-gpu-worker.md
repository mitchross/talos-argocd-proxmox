# Dell Proxmox GPU worker

The canonical architecture and deployment procedure lives in
[`docs/domains/networking/wifi-proxmox-talos-worker.md`](../../docs/domains/networking/wifi-proxmox-talos-worker.md).

Keep provider runtime examples and machine classes under `omni/`, but update
the canonical runbook whenever the Dell host, media-bridge networking, GPU
driver branch, provisioning sequence, or rollback procedure changes.
