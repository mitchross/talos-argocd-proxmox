# Dell Proxmox GPU worker

The canonical architecture and deployment procedure lives in
[`docs/domains/networking/dell-proxmox-talos-worker.md`](../../docs/domains/networking/dell-proxmox-talos-worker.md).

Keep provider runtime examples and machine classes under `omni/`, but update
the canonical runbook whenever the Dell host, its networking, provisioning
sequence, or rollback procedure changes.
