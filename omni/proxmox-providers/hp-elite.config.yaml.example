# Copy to hp-elite.config.yaml and fill in credentials. *.config.yaml is gitignored.
proxmox:
  # HP Elite Mini 600 G9 host (wired LAN).
  url: "https://192.168.10.22:8006/api2/json"
  insecureSkipVerify: true

  # Dedicated omni@pve account with the Administrator role at `/`.
  username: "omni"
  password: "CHANGE_ME"
  realm: "pve"
